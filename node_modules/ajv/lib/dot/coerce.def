{{## def.coerceType:
  {{
    var $dataType = 'dataType' + $lvl
      , $coerced = 'coerced' + $lvl;
  }}
  var  = typeof ;
  var  = undefined;

  
    if ( == 'object' && Array.isArray() && .length == 1) {
       = [0];
       = typeof ;
      if ()  = ;
    }
  

  if ( !== undefined) ;
  
    
      else if ( == 'number' ||  == 'boolean')
         = '' + ;
      else if ( === null)  = '';
    
      else if ( == 'boolean' ||  === null
          || ( == 'string' &&  &&  == +
           && !( % 1)))
         = +;
    
      else if ( === 'false' ||  === 0 ||  === null)
         = false;
      else if ( === 'true' ||  === 1)
         = true;
    
      else if ( === '' ||  === 0 ||  === false)
         = null;
    
      else if ( == 'string' ||  == 'number' ||  == 'boolean' ||  == null)
         = [];
    
  
  else {
    
  }

  if ( !== undefined) {
    
     = ;
    if ( !== undefined)
      [] = ;
  }
#}}
